# CLAUDE.md - ai-contact-docs

このリポジトリは Kotoba.AI（AI Contact）のユーザー向けドキュメントサイトです。Mintlify で構築されています。

## ドキュメント執筆の重要原則

### フロントエンドUIベースで書くこと（YAML記法で書かない）

**現状の問題**: ドキュメントが YAML ベースで記述されている（`steps:`, `- 指示:`, `- 待機:`, `saves:` など）。しかし、ユーザーは YAML を直接書くことはない。

**あるべき姿**: ドキュメントは**フロントエンドのUI上でどう操作するか**を基準に書くこと。

ユーザーが実際に使うのは Tiptap ベースの **Kotoba エディタ**。ドキュメントでは、実際の画面・ボタン・項目名に即してユーザーが操作する手順を書く。

具体的な UI 操作の記法（ステップの追加方法、ツール・フロー・変数の参照方法、設定ダイアログの開き方など）は頻繁に変わるため、本 CLAUDE.md では固定せず、**ダッシュボードの実画面と既存ドキュメントページの表現を確認して踏襲する**こと。

なお、`base_prompt` はエージェント設定画面の別セクションで編集する（Kotoba エディタの外）。

つまり、ドキュメントでは：
- ❌ ````yaml\nsteps:\n  - 指示: "..."` のようなYAMLコードブロック
- ✅ 実際の UI の画面名・ボタン名・項目名に即した操作手順の説明
- ✅ スクリーンショットやUIの図解

### その他の原則

- 対象読者は技術者ではなく、**ビジネスユーザー**（コールセンター管理者など）
- 専門用語を避け、平易な日本語で書く
- 各ページは短く簡潔に保つ

## リポジトリ構成

- `docs.json`: Mintlify の設定ファイル（ナビゲーション、テーマなど）
- `**/*.mdx`: ドキュメントページ（MDX形式）
- `images/`: スクリーンショット等の画像
- `logo/`: ロゴファイル

## 開発コマンド

```bash
# ローカルプレビュー
npx mintlify dev

# ビルド確認
npx mintlify build
```

## 関連リポジトリ

- `/home/yuki/ai-contact/frontend`: フロントエンド（エディタUIの実装はここ）
- `/home/yuki/ai-contact/backend`: バックエンドAPI
- `/home/yuki/ai-contact/backend-pipecat`: 音声処理バックエンド
